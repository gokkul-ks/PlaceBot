[pytest]
# Only collect tests from tests/. This keeps `pytest` (run from the
# project root) from also trying to collect test_setup.py, which is a
# standalone CLI diagnostic script rather than a pytest test module.
testpaths = tests
