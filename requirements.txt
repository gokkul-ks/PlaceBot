# PlaceBot - Python dependencies
# Install with: pip install -r requirements.txt
#
# These versions are known to work together on Python 3.10+.

# --- Web framework ---------------------------------------------------------
fastapi==0.129.0
uvicorn[standard]==0.40.0

# --- AI & NLP ----------------------------------------------------------------
sentence-transformers==5.2.2
scikit-learn==1.8.0

# --- Data processing ----------------------------------------------------------
pandas==3.0.0
numpy==2.4.2

# --- HTTP client (used to call a local Ollama server) --------------------------
requests==2.32.5

# --- Configuration via .env files ----------------------------------------------
python-dotenv==1.2.1
